# Generate these for production. Dev will auto-generate local files in instance/.
# python -c "import secrets; print(secrets.token_urlsafe(64))"
NORTHSTAR_SESSION_SECRET=
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
NORTHSTAR_FERNET_KEY=
NORTHSTAR_DB_PATH=instance/northstar.sqlite3
NORTHSTAR_UPLOAD_DIR=instance/uploads
